# === Database (Supabase) ===
supabase>=2.5.0

# === AI & RAG Processing ===
langchain-text-splitters>=0.1.2
pypdf>=4.2.0

# Thư viện này sẽ tự động kéo PyTorch về. 
# (Nhưng cần cài PyTorch CUDA thủ công trước nếu muốn dùng GPU, xem hướng dẫn dưới)
sentence-transformers>=3.0.1

# === Fix Lỗi & Ổn định (QUAN TRỌNG) ===
# Giữ NumPy < 2 để tránh crash các thư viện AI cũ
numpy<2.0.0

# Khóa bản này để sửa lỗi "AttributeError: GetPrototype" bạn vừa gặp
# Nếu project cần mediapipe mới hơn, có thể thử đổi thành: protobuf>=3.20.3,<5.0.0
protobuf==3.20.3

# === Tiện ích ===
tqdm>=4.66.0
langchain-community
tavily-python
pyjwt>=2.8.0
# === Testing ===
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-asyncio>=0.21.0